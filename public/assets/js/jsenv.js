// Only used as a fallback when the index view specifically states to load resources locally
// Otherwise these are loaded from the /jsenv view
var
    CONSOLE_FLAG_DEBUG = true,
    CONSOLE_FLAG_ERROR = true,
    CONSOLE_FLAG_INFO  = true,
    CONSOLE_FLAG_LOG   = true,
    CONSOLE_FLAG_WARN  = true,
    LOADING_SCREEN_ENABLED = true
;
